repos:
  - repo: local
    hooks:
      - id: release-hygiene
        name: Release hygiene (no db/pycache/pyc)
        entry: python scripts/check_release_hygiene.py
        language: system
        pass_filenames: false
