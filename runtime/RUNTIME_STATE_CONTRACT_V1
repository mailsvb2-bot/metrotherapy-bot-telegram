Runtime state contract v1

This release resolves mutable data, logs, caches, and temporary files outside the content-addressed runtime tree and is safe to start with ReadOnlyPaths applied to METRO_RUNTIME_ROOT.

Capability marker for deploy-time guard selection and read-only production topology probes.

Read-only probe generation: 900862a5.
Terminal probe generation: 2.
