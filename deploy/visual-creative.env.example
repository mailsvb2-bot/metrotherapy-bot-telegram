# Optional Visual Creative Engine client settings for Metrotherapy.
# Merge these values into the authoritative production environment only when
# the separately deployed Visual Creative Gateway is enabled.

# Explicit base capability flag for new deployments. When this flag is absent,
# an already configured gateway URL/token still enables the base capability for
# backward compatibility with deployments created before this flag existed.
VISUAL_CREATIVE_ENABLED=0

# Creative Studio/render-pack is deliberately a separate staged capability.
# Keep it disabled until the separately deployed gateway has the reviewed v5.1+
# render-pack API. This flag never enables implicitly from legacy gateway config.
VISUAL_CREATIVE_STUDIO_ENABLED=0

# Base URL of the independent gateway. A path prefix is allowed.
# Production/non-loopback endpoints are HTTPS-only by default.
VISUAL_GATEWAY_URL=https://visual-gateway.example.internal

# Explicit emergency/development escape hatch for a non-loopback http:// gateway.
# Leave disabled in production: prompts, generated media and bearer credentials
# would otherwise travel without TLS. Loopback localhost/127.0.0.0/8/::1 is
# allowed without this flag for local development.
VISUAL_GATEWAY_ALLOW_INSECURE_HTTP=0

# Per-application bearer token issued by the gateway. Never commit a real token.
VISUAL_GATEWAY_TOKEN=

# Two-letter deployment country used by the gateway's provider policy (for
# example RU/NL/US). It is required whenever the base capability is enabled and
# is also bound into Studio experiment/variant identity.
VISUAL_DEPLOYMENT_COUNTRY=RU

# Optional deterministic Metrotherapy composition colors. These affect Studio
# variant identity/idempotency so changing a value creates a new realization.
METRO_CREATIVE_PRIMARY_COLOR=#141A24
METRO_CREATIVE_ACCENT_COLOR=#D9B56D
METRO_CREATIVE_TEXT_COLOR=#FFFFFF

# How long Telegram commands may wait synchronously before returning a job id.
# Accepted by the client: 0..60 seconds.
VISUAL_TELEGRAM_WAIT_SECONDS=20

# Transport and response limits. These protect the bot process from a broken or
# malicious gateway response; they do not replace gateway-side quotas.
VISUAL_GATEWAY_TIMEOUT_SECONDS=30
VISUAL_GATEWAY_MAX_JSON_BYTES=1048576
VISUAL_GATEWAY_MAX_MEDIA_BYTES=268435456

# Temporary download location. Generation and render media are streamed to a
# unique temporary file, atomically materialized only after trust checks pass,
# then removed after the Telegram upload attempt.
VISUAL_CREATIVE_OUTPUT_DIR=data/visual_creatives
