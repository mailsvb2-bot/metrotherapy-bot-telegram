name: Postgres Smoke

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  baseline:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Green Postgres smoke baseline marker
        run: |
          echo "POSTGRES_SMOKE_GREEN_BASELINE_OK"
          test -f README.md
