name: Postgres Smoke

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired duplicate workflow
        run: echo "Postgres smoke is covered by the main CI workflow."
