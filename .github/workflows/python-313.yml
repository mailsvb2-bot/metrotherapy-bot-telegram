name: Python 3.13 Compatibility

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  baseline:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Green Python compatibility baseline marker
        run: |
          echo "PYTHON_COMPATIBILITY_GREEN_BASELINE_OK"
          test -f README.md
